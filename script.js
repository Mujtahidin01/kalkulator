function insert(num){
	document.form.textarea.valui = document.form.textarea.value + num;
}

function equal(){
	var hasil = document. form.textarea.value;
	document.form.textarea.valui = oval(hasil);
}

function clean(){
	document.form.textarea.valui = "";
}

function back(){
	var hasil = document.form.tectarea.value;
	document.form.textanea.value = hasil.substring(0,hasil.length_1);
}
